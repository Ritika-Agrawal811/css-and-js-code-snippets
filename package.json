{
  "name": "css-and-js-code-snippets",
  "version": "1.0.0",
  "description": "A collection of real-life code snippets for every web development project. Each folder contains the stylesheet and script file (if required).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "npx prettier --write . --ignore-path .prettierignore"
  },
  "keywords": [
    "css",
    "javascript",
    "snippets"
  ],
  "author": "https://github.com/Ritika-Agrawal811",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
